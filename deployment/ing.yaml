apiVersion: apps/v1
kind: Deployment
metadata:
  name: psn-wa-deployment
spec:
  replicas: 1
  selector:
    matchLabels:
      app: psn-wa
  template:
    metadata:
      labels:
        app: psn-wa
    spec:
      containers:
        - name: psn-wa
          image: julian9999/psn-wa:v1.0
          ports:
            - containerPort: 4600

---

apiVersion: v1
kind: Service
metadata:
  name: psn-wa-service
spec:
  selector:
    app: psn-wa
  ports:
  - name: http
    protocol: TCP
    port: 4600
    targetPort: 4600
  type: LoadBalancer

---